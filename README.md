# Samuright

A mobile-first Japanese vocabulary slash game prototype built with React, TypeScript, Phaser, and Vite.

## Run locally

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
Kanji and Vocabulary memorisation game
